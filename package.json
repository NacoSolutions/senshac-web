{
  "name": "senshac-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "dev": "tinacms dev -c \"astro dev\"",
    "test": "node --test",
    "lint": "node scripts/lint.mjs",
    "typecheck": "astro check",
    "preview:check": "node scripts/preview-check.mjs",
    "repository:check": "node scripts/repository-check.mjs",
    "quality": "npm run lint && npm test && npm run typecheck && npm run build && npm run preview:check && npm run repository:check"
  },
  "dependencies": {
    "@tinacms/astro": "0.7.0",
    "astro": "5.14.1",
    "tinacms": "3.14.0"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.4",
    "@tinacms/cli": "3.0.0",
    "typescript": "5.9.2"
  }
}
